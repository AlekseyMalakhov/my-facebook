type Query {
    user(id: ID!): User
}

type Mutation {
    addUser(input: AddUserInput!): User
}

type User {
    id: ID!
    name: String!
    surname: String!
    profession: String!
}

input AddUserInput {
    name: String!
    surname: String!
    profession: String!
}
